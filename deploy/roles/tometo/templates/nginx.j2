server {
  server_name {{ tm_hostname }};
  root /var/www/{{ checkout_dir }}/dist/;
  index index.html;

  location / {
    default_type "text/html";
    try_files $uri.html $uri uri/ /index.html;
  }
}
