[tometo]
hostname = {{ tm_hostname }}

[otemot]
database_url = {{ database_url }}
storage = "local"
google_credentials = {{ google_creds }}
hostname = {{ hostname }}
external_url = {{ ot_hostname }}
port = {{ port }}
env = {{ env }}

[otemot.secrets]
cookie = {{ cookie_secret }}
jwt = {{ jwt_secret }}
