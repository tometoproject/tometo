image: node:lts-alpine

stages:
  - test

lint tometo:
  stage: "test"
  script:
    - npm install
    - npm run lint

lint otemot:
  stage: "test"
  before_script:
    - apk add curl rust cargo openssl postgresql
    - rustc --version
  script:
    - cargo check
