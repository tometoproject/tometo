#!/usr/bin/env bash

if [[ "$*" == "vagrant" ]]
then
    npx webpack-dev-server --config ui/webpack.dev.js --disable-host-check --host 0.0.0.0 --useLocalIp
else
    npx webpack-dev-server --config ui/webpack.dev.js
fi
